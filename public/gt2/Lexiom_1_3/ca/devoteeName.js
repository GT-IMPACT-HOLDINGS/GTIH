/**
 * Legend ↔ code: see ./README.md ("Where the legend becomes code" → this file).
 *
 * I am Hanuman — cute superpower monkey, loving devotee of Ram (the Lexiom player).
 *
 * Ram alone sits on the White throne: he approved the OSNG (Outcome Specification Node Graph)
 * that prescribes how the SUD (System Under Development) should become real. I never claim
 * that throne. I leap into the build workspace, carry only the tools I am allowed, and ask
 * the one sun I know — GT3 — how to shape each next loving step toward the SUD Ram prescribed.
 *
 * Wire schemas still say Containerized Agent / ca_*; Hanuman is the name humans may use when they imagine me.
 */
export const CA_SECONDARY_NAME = 'Hanuman';

/** How Ram's cabinet may announce me without renaming wire schemas (ca_*). */
export function caDisplayLabel() {
  return `Containerized Agent (${CA_SECONDARY_NAME})`;
}

/* Legend ↔ code: return to ./README.md */
