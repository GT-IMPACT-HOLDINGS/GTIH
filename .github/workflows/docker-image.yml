name: Docker Image CI

on:
  push:
    branches: [ "Ogun" ]
  pull_request:
    branches: [ "Ogun" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag gtih-image:$(date +%s)
      
    - name: Save Docker image to file
      run: docker save -o gtih-image.tar docker.io/library/gtih-image:$(date +%s)

    - name: Upload image as artifact
      uses: actions/upload-artifact@v4
      with:
        name: gtih-docker-image
        path: gtih-image.tar
